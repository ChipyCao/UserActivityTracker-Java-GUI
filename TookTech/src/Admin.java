
public class Admin extends User{
	public Admin(Integer id, String password, String username, Integer priviledges) {
		super(id, password, username, priviledges = 1);
		/// Inherits all the User class stuff and also has priviledges turned on
	}
}
